<%= @animal.name %>
<%= link_to "Edit", edit_animal_path(@animal) %>
<%= link_to "Destroy", animal_path(@animal), method: :delete, data: { confirm: "Are you sure?" } %>
<br>
<%= render @animal.sightings %>
Create a new sighting:
<%= render 'sightings/form' %>
